{# templates/partials/_cta.html.twig #}
<section class="relative bg-base-200 py-20 text-center">
    <div class="max-w-2xl mx-auto px-6">

        {# Small animated decorative icon #}
        <div class="flex justify-center mb-4">
            <i class="fas fa-star text-primary text-4xl animate-bounce"></i>
        </div>

        {# Title #}
        <h2 class="text-3xl md:text-4xl font-bold mb-6 text-base-content">
            Ready when you are
        </h2>

        {# Main description #}
        <p class="text-lg text-base-content/80 mb-4">
            Taskio is built for teams that want to move fast, work smart, and stay aligned.
            Sign up, create your first board, and discover a new way to work.
        </p>

        {# Tagline #}
        <p class="text-base text-base-content/80 font-semibold mb-8">
            <span class="text-primary font-bold">Taskio.</span> Your board, your power.
        </p>

        {# CTA button #}
        <a
            href="#"
            class="inline-block bg-primary text-base-100 font-medium py-3 px-6 rounded-lg transition hover:bg-primary/90"
        >
            Now, let’s build something great.
        </a>
    </div>

    {# Subtle background decoration #}
    <div class="absolute inset-0 pointer-events-none">
        <div class="w-64 h-64 bg-primary/10 rounded-full blur-3xl -top-10 -left-10 absolute"></div>
        <div class="w-64 h-64 bg-secondary/10 rounded-full blur-3xl -bottom-10 -right-10 absolute"></div>
    </div>
</section>



